# Nexa — copy to .env and fill in. Never commit .env.

# Model provider. Only the active provider's key is required.
ANTHROPIC_API_KEY=

# Which provider the composition root binds at startup.
NEXA_MODEL_PROVIDER=anthropic
NEXA_MODEL_ID=claude-opus-5

# HTTP
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

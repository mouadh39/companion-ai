# Visual QA — 2026-09-06 (DesignSync reconciliation pass)

Method: `test/golden/screens_golden_test.dart` renders each core screen straight
from `NexaHome` at 390×844, Light and Dark, from the app's real widgets — 20
golden PNGs in `test/golden/goldens/`. Refresh with
`flutter test --update-goldens test/golden/`.

Text renders in the flutter_test fallback font (Satoshi/Manrope are not vendored),
so these verify **composition, spacing, hierarchy, material, colour and
Light/Dark parity** — not glyph shapes. Compared against the authoritative
`NexaPhone.dc.html` and the reference `screenshots/**` in those terms.

Live-browser capture (the prior QA method, headless Chrome under a phone UA) was
**not available this session** — the Claude-in-Chrome extension was offline. The
`?qa=<screen>&theme=<light|dark>` URL hook is in place on the web build for when
it is.

| Screen | Expected (authoritative) | Actual (golden) | Delta | Status |
|---|---|---|---|---|
| **Entrance** | mark arrives (fade+rise+scale), orientation drift, emerald light pass masked to the mark, breathing, hand-off; 2.8 / 0.9 / 0.8s | new `EntranceScreen`, all four elements + reduced-motion; routing behind it | animated — not a still golden; covered by `entrance_screen_test.dart` (4 tests) | ✅ implemented |
| **Welcome** | mark + bloom + halo centred, headline, body, primary + quiet CTA | matches — mark 232 w/ bloom, halo, "Get started" ink pill + "I already have an account" | copy: "Explore what's next." vs board's "A more present you." (kept existing) | ✅ match |
| **Sign in** | back, mark+wordmark, title/subtitle, 4 silhouette provider rows on frosted surface, "or use email" rule, boxed email form, footer switch | matches — Google/Apple/Meta/Passkey silhouettes, divider, boxed Email/Password w/ focus ring, "Forgot password?", switch link | provider tap → honest inline notice (no fake nav) | ✅ match + honest |
| **Sign up** | as sign in + Confirm password; outcome → "check your email" emerald card | matches — 3rd boxed field, `_ConfirmEmailNotice` emerald card | — | ✅ match |
| **Forgot password** | one email field, one action, sent state | matches — boxed field, "Send reset link" (spinner when busy), emerald sent card | — | ✅ match |
| **Assistant** | greeting kicker + presence line, account glyph, centred mark (162/176/184, bloom 54/78/86), Memories glass card + 62px emerald mic, tab bar | matches closely — same sizes/bloom, `NexaListeningRing`, mic dot→square, tab bar active pill | idle shows nothing under the mark; ref shows "TAP TO SPEAK". live chip is a bare label vs ref's bordered emerald pill w/ dot. account glyph is "⋯" vs ref person-outline | 🟡 minor — 3 small deltas noted |
| **Memory (empty)** | dormant mark (graphite dark / stealth-black light) .55 breathing, "Nexa hasn't remembered anything yet.", "Talk to Nexa" emerald pill | matches — dormant material now theme-switched, .55, no glow; `NexaEmeraldButton` | skeleton loading state has no shimmer pulse (ref has `nx-shimmer`) | 🟡 minor — shimmer |
| **Devices (empty)** | "Add device" emerald card, then empty sheet: dormant mark .55, "No devices yet.", "Add your first device" ink pill, footnote | matches — dormant material theme-switched | stealth-black at .55 on white reads faint; could go .65 | 🟡 minor — opacity |
| **Account** | avatar + "Your account" + "Set up your profile" emerald pill (→ onboarding), "You and Nexa" emerald card w/ real 0/0, grouped nav, "Log out" | matches — set-up-profile pill added, real zeroes, Profile row "Not set up", groups, danger Log out | ref rows carry a leading icon circle; `ProfileScreen` groups pass none (icons present on the deeper Account/Settings screens) | 🟡 minor — row icons on the You root |
| **Settings** | back → Account, title/subtitle, **System/Light/Dark segmented control promoted to top** w/ live note, grouped Experience/Memory/Devices, footer wordmark + tagline | matches — segmented control added at top on a solid `--group` card w/ per-choice note; grouped rows | footer wordmark + "Personal AI · Spatial computing" tagline not yet added; sub-screen tree kept (deeper than ref's flat shape, no reference demand to remove it) | 🟡 minor — footer |
| **Appearance** | mark preview, Theme / Material / Motion groups | matches — unchanged; the promoted control on Settings is additive | — | ✅ match |

## Tokens (verified against the authoritative DARK/LIGHT maps)

- `--void` LIGHT reconciled `#F3F4F8` → `#EDF2EE` (warm) — visible in every light golden as a warmer, greener off-white ground.
- Quiet ink steps stepped up on LIGHT (55→.58, 45→.52, …) — muted metadata now reads on white.
- New `group` token — `NexaGroup` is a solid near-white `.86` card on light / quiet `.045` wash on dark with a `--hairline-strong` edge, distinct from the glass wash (glass reserved for floating surfaces). Visible in settings-light / account goldens.
- DARK map was already spot-on; unchanged.

## Remaining visual deltas — status after the finalization pass (2026-09-06 pm)

1. Assistant idle "TAP TO SPEAK" hint under the mark. — ✅ done (prior pass, commit `e247abb`).
2. Assistant live chip → bordered emerald-wash pill with a glowing dot. — ✅ done (prior pass, `e247abb`).
3. Assistant account button → person-outline glyph. — ✅ **done this pass**: `_RoundIconButton` now draws `NexaGlyph(NexaIcon.profile)` (the same outline the deeper screens use) instead of the "⋯" text glyph. Golden: `assistant-{dark,light}` refreshed.
4. Memory loading skeleton → `nx-shimmer` opacity pulse. — ✅ **done this pass**: `_MemorySkeleton` is now stateful and breathes on one 1.5 s opacity cycle (0.55 ↔ 1.0), held still under reduced motion. Not a still golden (the skeleton only shows for ~420 ms of load); verified by running the app.
5. Devices/Memory empty-state dormant mark opacity on white. — ✅ **done this pass**: `opacity: c.isDark ? 0.55 : 0.62` on both empty states — graphite stays .55 on the near-black ground, stealth-black lifts to .62 on the off-white one. Goldens: `devices-empty-light`, `memory-empty-light` refreshed; the dark goldens are unchanged, as intended.
6. Settings footer → wordmark (.34) + "Personal AI · Spatial computing". — ✅ done (prior pass, `e247abb`).
7. `ProfileScreen` (You root) group rows → leading icon circles. — ✅ **done this pass**: every row in the "Nexa" and "Account" groups and the Settings/About group now carries a `NexaIcon` (voice / theme / memory / devices / profile / notifications / privacy / data / settings / help), matching the icon-per-row rhythm the `AccountSettingsScreen` and `SecurityScreen` already use. Goldens: `account-{dark,light}` refreshed.
8. **Wordmark PNG — NOT shipped. External blocker.** The approved `assets/marks/wordmark-{white,ink}.png` exist in the DesignSync project, but the only transfer path available here (DesignSync `get_file` → base64 → local file) corrupted the binary on two attempts (both Flutter's and PIL's decoders rejected the result; `file(1)` recognised only the PNG header). `NexaWordmark` therefore stays text-rendered in `NexaType.brand` — the documented Satoshi-substitute stance. **To finish this:** copy the two PNGs from the design project into `apps/flutter-client/assets/marks/` with real file access, then point `NexaWordmark` at `wordmark-white.png` tinted via `Image(color:, colorBlendMode: BlendMode.srcIn)` (one alpha master serves both themes) and precache it in `screens_golden_test.dart`.

Everything except #8 is a targeted correction, not a redesign. #8 needs a file a human can drop in.
